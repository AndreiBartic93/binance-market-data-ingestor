#Docker specific Spring Boot configuration.
#This profile is enabled from docker-compose.yml using: SPRING_PROFILES_ACTIVE=docker

spring:
  application:
    name: binance-market-data-ingestion

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
